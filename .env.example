# API base URL (no trailing slash). Local dev uses Vite proxy via /api.
# Production: point to your hosted JSON Server or MockAPI endpoint.
VITE_API_URL=/api/books

# Optional: direct URL when not using proxy (e.g. MockAPI)
# VITE_API_URL=https://YOUR_PROJECT_ID.mockapi.io/v1/books
